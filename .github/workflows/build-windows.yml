name: Build Windows GPU Cast

on:
  push:
    branches: [main, "agent/**"]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: never
  CARGO_NET_RETRY: 10
  CARGO_HTTP_MULTIPLEXING: false

jobs:
  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v7

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc

      - name: Prepare patched upstream source
        shell: pwsh
        run: ./scripts/prepare-source.ps1

      - name: Cache Cargo and target
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            .work/stream-server/target
          key: windows-rust-${{ hashFiles('UPSTREAM_COMMIT', 'overrides/**', 'scripts/prepare-source.ps1') }}
          restore-keys: |
            windows-rust-

      - name: Build release binary
        shell: pwsh
        working-directory: .work/stream-server
        run: |
          cargo build --quiet --message-format short --release -p server --target x86_64-pc-windows-msvc --no-default-features --features librqbit 2>&1 | Tee-Object cargo-build.log
          if ($LASTEXITCODE -ne 0) {
            Write-Host "=== Rust compiler diagnostics ==="
            Get-Content cargo-build.log
            exit $LASTEXITCODE
          }

      - name: Upload compiler log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cargo-build-log
          path: .work/stream-server/cargo-build.log
          if-no-files-found: ignore

      - name: Upload Windows binary
        uses: actions/upload-artifact@v7
        with:
          name: stream-server-gpu-cast-windows-amd64
          path: .work/stream-server/target/x86_64-pc-windows-msvc/release/server.exe
          if-no-files-found: error
